{
  "name": "phaser-project",
  "version": "1.0.0",
  "main": "./Project/index.html",
  "description": "Prototype phaser project",
  "repository": {
    "type": "git",
    "url": "git://github.com/Sulfastic/phaser-prototype.git"
  },
  "scripts": {
    "start": "grunt connect:example",
    "clean": "rm -rf dist",
    "dist": "mkdir dist",
    "copy": "cp -R src/* dist/",
    "build": "npm run clean && npm run dist && npm run copy",
    "test": "echo \"No tests yet :P\" && exit 0"
  },
  "author": "Sulf",
  "license": "ISC",
  "dependencies": {
    "requirejs": "^2.3.2"
  },
  "devDependencies": {
    "grunt": "~0.4.0",
    "grunt-connect": "^0.2.0",
    "grunt-contrib-jshint": "~0.10.0",
    "grunt-contrib-nodeunit": "~0.4.1",
    "grunt-contrib-uglify": "~0.5.0"
  }
}
